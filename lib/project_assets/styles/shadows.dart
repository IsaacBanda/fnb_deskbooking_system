import 'package:fnb_deskbooking_system/exports/export.dart';

class AppShadows {
  /// our shadow colour for our whit components
  static const boxShadow = BoxShadow(
    color: Color.fromARGB(58, 57, 72, 99),
    offset: Offset(0, 4),
    blurRadius: 4,
    spreadRadius: -3,
  );

  AppShadows._();
}
